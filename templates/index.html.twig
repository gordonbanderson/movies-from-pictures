<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>

    <style>
        ul {
            list-style-type: none;
            clear: both;
            padding-bottom: 50px;
        }
        li {
            float: left;
            display: inline-block;
            padding-right: 4px;
        }
    </style>
</head>
<body>
<h1>{{ title }}</h1>

{% for bucket in buckets %}
    <ul>
    {% for photo in bucket %}
        <li><img src="./thumbs/{{  photo.filename }}" /></li>
    {% endfor %}
    </ul>

{% endfor %}


</body>
</html>